#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - main entry
* description:sa
*
* Return: always 0
*/
int main(void)
{
char ch;
for(ch = 'A' ; ch <= 'Z' ; ch++)
{
putchar(ch);
}
return (0);
}
